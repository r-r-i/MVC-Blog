<h2>HOME PAGE</h2>

{{#each blogs as |blog|}}
    <div>
        <div>
            <h1>{{blog.title}}</h1>
            <p>author: {{user.username}} at {{blog.createdAt}}</p>
            <p>{{blog.content}}
            <p>{{blog.comment}}</p>
        </div>
    </div>
{{/each}}

   
